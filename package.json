{
    "name": "FGProgressKnight",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "nuxt",
        "build": "nuxt build",
        "start": "nuxt start",
        "generate": "nuxt generate",
        "deploy": "git add -A && git commit -m \"dev\" && git push && npm run generate && push-dir --dir=dist --branch=gh-pages --cleanup && npm run dev"
    },
    "dependencies": {
        "@nuxtjs/i18n": "^7.2.0",
        "bootstrap": "^5.1.3",
        "core-js": "^3.15.1",
        "lz-string": "^1.4.4",
        "nuxt": "^2.15.7"
    },
    "devDependencies": {
        "push-dir": "^0.4.1",
        "sass": "^1.43.4",
        "sass-loader": "^10.2.0"
    }
}
